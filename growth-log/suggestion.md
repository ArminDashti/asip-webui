# Suggestions

- Add a small link between `/` and `/ip` once users need discovery without remembering the URL.
- Share a single result-summary component between HomePage and IpPage to avoid duplicated markup.
- Optional light client-side IPv4/IPv6 validation before calling the API.
